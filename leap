#include<stdio.h>
void main()
{
    int y;
    printf("enter a year\n");
    scanf("%d",&y);
    if(y%4==0)
    {
        printf("it is a leap year");
    }
    else
    {
        printf("it is not a leap year");
    }
}
